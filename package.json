{
  "name": "partial-existence-backend",
  "version": "1.0.0",
  "description": "Cloudflare Pages Serverless Edge Functions with MongoDB for Partial Existence Blog",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "server": "node server.js",
    "dev": "wrangler pages dev public --compatibility-flags=nodejs_compat",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^3.111.0"
  },
  "dependencies": {
    "mongodb": "^6.14.0"
  }
}
